# Dual Momentum / Trend Core Strategy — v1.0.0
# Academic basis: Antonacci (2014) Dual Momentum
# 17.4% CAGR, Sharpe 0.87, max drawdown 22.7% (1974-2013)

version: "1.0.0"
name: dual_momentum_trend_core
description: >
  Dual momentum with absolute and relative momentum signals.
  Holds the highest-ranked asset when absolute momentum is positive,
  otherwise rotates to cash proxy (SHY).

instruments:
  risk_assets:
    - SPY   # S&P 500
    - QQQ   # Nasdaq 100
  safe_assets:
    - AGG   # US Aggregate Bond
    - SHY   # 1-3 Year Treasury (cash proxy / T-bill proxy)

signals:
  lookback_days: 252              # 12-month return (~252 trading days)
  benchmark: SHY                  # T-bill proxy for absolute momentum
  absolute_momentum: true         # Compare return vs T-bills
  relative_momentum: true         # Rank assets by return

regime_filter:
  enabled: true
  indicator: spy_200d_sma         # Hold cash when SPY below 200-day SMA
  reference_symbol: SPY

allocation:
  capital_pct: 0.60               # 60% of total portfolio

rebalance:
  frequency: monthly
  expected_turnover: "1-2 trades per month"
  typical_holding_period: "1-6 months"
